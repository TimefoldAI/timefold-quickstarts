########################
# Timefold Solver properties
########################

# The solver runs for 30 seconds. To run for 5 minutes use "PT5M" and for 2 hours use "PT2H".
timefold.model.termination.spent-limit=PT30S

# Temporary comment this out to detect bugs in your code (lowers performance)
# quarkus.timefold.solver.environment-mode=FULL_ASSERT

# Temporary comment this out to return a feasible solution as soon as possible
# timefold.model.termination.best-score-limit=[0]hard/[*/*/*]soft
# quarkus.timefold.solver.termination.best-score-limit=[0]hard/[*/*/*]soft

# To see what Timefold is doing, turn on DEBUG or TRACE logging.
quarkus.log.category."ai.timefold.solver".level=INFO
# For TRACE logging also activate this:
# quarkus.log.min-level=TRACE
%test.quarkus.log.category."ai.timefold.solver".level=INFO
%prod.quarkus.log.category."ai.timefold.solver".level=INFO

########################
# Model information
########################

timefold.model.id=task-assigning
quarkus.application.name=${timefold.model.id}
timefold.model.name=Task Assigning
model.api.version=v1
timefold.model.api-version=${model.api.version}

timefold.model.contact.email=example@acme.com
timefold.model.contact.name=A.C.M.E.
timefold.model.contact.url=https://acme.com
timefold.model.maturity-level=Example

timefold.model.features[0]=Only assign a task to an employee who has every skill the task requires.
timefold.model.features[1]=Try to assign every task to an employee, rather than leaving it unassigned.
timefold.model.features[2]=Minimize the makespan, so the employee who finishes last finishes as early as possible.
timefold.model.features[3]=Finish critical priority tasks as early as possible.
timefold.model.features[4]=Finish major priority tasks as early as possible.
timefold.model.features[5]=Finish minor priority tasks as early as possible.

########################
# Model settings
########################

timefold.model.max-thread-count=16
timefold.model.default-config.max-thread-count=1

########################
# Native build properties
########################

# Enable Swagger UI also in the native mode
quarkus.swagger-ui.always-include=true

########################
# Test overrides
########################

# Effectively disable spent-time termination in favor of the best-score-limit
%test.timefold.model.termination.spent-limit=PT1H
# A solution with nothing assigned yet trivially has 0 hard (missing skills only count against an
# assigned task), so constraining only the hard level would let the solver terminate before ever
# placing a single task. The first soft level (minimize unassigned tasks) is constrained to 0 as
# well to force it to actually assign every task; the remaining soft levels (makespan, priority end
# times) are left wildcarded, since tests only assert feasibility and full assignment, not an
# optimal soft score.
%test.timefold.model.termination.best-score-limit=[0]hard/[0/*/*]soft
%test.quarkus.timefold.solver.termination.best-score-limit=[0]hard/[0/*/*]soft
