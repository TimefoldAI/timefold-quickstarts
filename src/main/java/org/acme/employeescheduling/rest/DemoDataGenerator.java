package org.acme.employeescheduling.rest;

import java.util.logging.Logger;

/**
 * Minimal, valid placeholder for the original DemoDataGenerator.
 * Keeps the class available for the build. If you want the original
 * demo-data seeding behavior restored, paste the original file contents
 * or tell me what it should do and I'll re-implement it.
 */
public class DemoDataGenerator {
    private static final Logger LOGGER = Logger.getLogger(DemoDataGenerator.class.getName());

    public DemoDataGenerator() {
    }

    /**
     * Optional init method you can call from bootstrap code.
     * Left intentionally simple to avoid new dependencies.
     */
    public void initialize() {
        LOGGER.info("DemoDataGenerator initialized.");
    }
}
